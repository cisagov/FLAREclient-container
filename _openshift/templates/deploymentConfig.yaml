---
apiVersion: v1
kind: Template
metadata:
  name: deployment-config-template
objects:
  - apiVersion: v1
    kind: DeploymentConfig
    metadata:
      name: humanreview-db
    spec:
      replicas: 1
      selector:
        name: humanreview-db
      strategy:
        type: Rolling
        rollingParams:
      template:
        metadata:
          labels:
            name: humanreview-db
        spec:
          containers:
          - env:
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: humanreview-db
                  key: username
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: humanreview-db
                  key: password
            imagePullPolicy: Always
            name: humanreview-db
            image: ${DOCKER_REPO_URL}/${IMAGE_NAMESPACE}/humanreview-db:${IMAGE_VERSION}
          serviceAccountName: "database-sa"
          imagePullSecrets:
            - name: artifactory-puller
      triggers:
      - type: ConfigChange
parameters:
  - name: IMAGE_NAMESPACE
    displayName: Docker image namespace
    description: The docker namespace that the image belongs to
    required: true
  - name: IMAGE_VERSION
    displayName: Image Version
    description: Docker image version
    required: true
  - name: DOCKER_REPO_URL
    displayName: Docker Repo URL
    description: Docker repository URL
    required: true
